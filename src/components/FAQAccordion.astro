---
const { items = [] } = Astro.props;
---
<div class="faq">
  {items.map((q) => (
    <details class="faq-item">
      <summary data-event="faq_open" data-page={Astro.url.pathname}>{q.q}</summary>
      <div class="answer" set:html={q.a} />
    </details>
  ))}
</div>
