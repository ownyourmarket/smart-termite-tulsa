---
const {
  heading = 'Not sure if it is termites?',
  sub = 'Request an inspection and get clear next steps from a local professional.',
  primary = { href: '/contact', label: 'Request Inspection' },
  secondary = { href: '/tulsa-termite-warning-signs', label: 'See Warning Signs' },
} = Astro.props;
---
<section>
  <div class="container">
    <div class="checklist-cta">
      <h2>{heading}</h2>
      <p class="muted" style="max-width:640px;margin:0 auto 22px;">{sub}</p>
      <div style="display:flex;gap:12px;justify-content:center;flex-wrap:wrap;">
        <a href={primary.href} class="btn btn-primary" data-event="cta_click" data-lead-type="request_inspection">{primary.label}</a>
        <a href={secondary.href} class="btn btn-secondary" data-event="cta_click">{secondary.label}</a>
      </div>
    </div>
  </div>
</section>
